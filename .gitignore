# --- BLOCK EVERYTHING BY DEFAULT ---
# (We will explicitly allow only HSI project files below)

# OpenClaw System Files (NEVER commit these)
.openclaw/
SOUL.md
AGENTS.md
USER.md
TOOLS.md
IDENTITY.md
BOOTSTRAP.md
HEARTBEAT.md

# Node.js dependencies
node_modules/

# Logs and temp files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables and secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.key
*.pem
credentials.json
# Note: config.json is NOT ignored here as it's the app config for HSI viewer

# OS files
.DS_Store
Thumbs.db